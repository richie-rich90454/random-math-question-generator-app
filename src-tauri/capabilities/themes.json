{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "theme-capability",
    "description": "Allows the app to change the native window theme",
    "windows": [
        "main"
    ],
    "permissions": [
        "core:window:allow-set-theme"
    ]
}